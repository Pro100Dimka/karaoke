# Зависимости backend-слоя (API/БД). Зависимости самого AI-пайплайна
# (librosa/demucs/whisper/pretty_midi/...) описаны отдельно в
# AI/requirements.txt — устанавливаются так же через pip install -r,
# оставлены раздельно, чтобы backend можно было поднять и без тяжёлых
# ML-зависимостей (для разработки API/БД без прогона пайплайна).

fastapi
uvicorn[standard]
python-multipart      # нужен FastAPI для приёма файлов (UploadFile)
sqlalchemy
pydantic

# Запись голоса и работа с аудио-устройствами
sounddevice
soundfile
numpy

-r AI/requirements.txt
